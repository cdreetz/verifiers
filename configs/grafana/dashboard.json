{
  "__inputs": [
    {
      "name": "DS_PROMETHEUS",
      "label": "Prometheus",
      "description": "Prometheus datasource for verifiers metrics",
      "type": "datasource",
      "pluginId": "prometheus",
      "pluginName": "Prometheus"
    }
  ],
  "annotations": {
    "list": []
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 0 },
      "id": 100,
      "title": "Overview",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "s" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 1 },
      "id": 1,
      "options": { "bucketSize": "" },
      "title": "Generate Duration",
      "type": "histogram",
      "targets": [
        {
          "expr": "rate(vf_generate_duration_sum[5m]) / rate(vf_generate_duration_count[5m])",
          "legendFormat": "avg generate duration"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 1 },
      "id": 2,
      "title": "Active Rollouts",
      "type": "timeseries",
      "targets": [
        {
          "expr": "vf_rollout_active",
          "legendFormat": "active rollouts"
        }
      ]
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 9 },
      "id": 101,
      "title": "Rollout Performance",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "s" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 10 },
      "id": 3,
      "title": "Rollout Duration (p50 / p95 / p99)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.50, rate(vf_rollout_duration_bucket[5m]))",
          "legendFormat": "p50"
        },
        {
          "expr": "histogram_quantile(0.95, rate(vf_rollout_duration_bucket[5m]))",
          "legendFormat": "p95"
        },
        {
          "expr": "histogram_quantile(0.99, rate(vf_rollout_duration_bucket[5m]))",
          "legendFormat": "p99"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "s" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 10 },
      "id": 4,
      "title": "Group Duration (p50 / p95 / p99)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.50, rate(vf_group_duration_bucket[5m]))",
          "legendFormat": "p50"
        },
        {
          "expr": "histogram_quantile(0.95, rate(vf_group_duration_bucket[5m]))",
          "legendFormat": "p95"
        },
        {
          "expr": "histogram_quantile(0.99, rate(vf_group_duration_bucket[5m]))",
          "legendFormat": "p99"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "s" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 10 },
      "id": 5,
      "title": "Idle Duration Between Completions",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.50, rate(vf_idle_duration_bucket[5m]))",
          "legendFormat": "p50 idle"
        },
        {
          "expr": "histogram_quantile(0.95, rate(vf_idle_duration_bucket[5m]))",
          "legendFormat": "p95 idle"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 18 },
      "id": 6,
      "title": "Rollout Throughput (per minute)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(vf_rollout_count_total[1m]) * 60",
          "legendFormat": "rollouts/min"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 18 },
      "id": 7,
      "title": "Group Throughput (per minute)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(vf_group_count_total[1m]) * 60",
          "legendFormat": "groups/min"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "s" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 18 },
      "id": 8,
      "title": "Scoring Duration",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.50, rate(vf_scoring_duration_bucket[5m]))",
          "legendFormat": "p50 scoring"
        },
        {
          "expr": "histogram_quantile(0.95, rate(vf_scoring_duration_bucket[5m]))",
          "legendFormat": "p95 scoring"
        }
      ]
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 26 },
      "id": 102,
      "title": "Token Usage",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 27 },
      "id": 9,
      "title": "Input Tokens (rate/min)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(vf_tokens_input_total[1m]) * 60",
          "legendFormat": "input tokens/min"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 27 },
      "id": 10,
      "title": "Output Tokens (rate/min)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(vf_tokens_output_total[1m]) * 60",
          "legendFormat": "output tokens/min"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 27 },
      "id": 11,
      "title": "Total Tokens (cumulative)",
      "type": "stat",
      "targets": [
        {
          "expr": "vf_tokens_total_total",
          "legendFormat": "total tokens"
        }
      ]
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 35 },
      "id": 103,
      "title": "Model / API Requests",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "s" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 36 },
      "id": 12,
      "title": "Model Request Latency (p50 / p95)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.50, rate(vf_model_request_duration_bucket[5m]))",
          "legendFormat": "p50"
        },
        {
          "expr": "histogram_quantile(0.95, rate(vf_model_request_duration_bucket[5m]))",
          "legendFormat": "p95"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 36 },
      "id": 13,
      "title": "Model Request Rate",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(vf_model_request_count_total[1m]) * 60",
          "legendFormat": "requests/min"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 36 },
      "id": 14,
      "title": "Model Request Errors",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(vf_model_request_error_total[1m]) * 60",
          "legendFormat": "{{error_type}}"
        }
      ]
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 44 },
      "id": 104,
      "title": "Browser Infrastructure",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 6, "x": 0, "y": 45 },
      "id": 15,
      "title": "Browser Sessions Created",
      "type": "stat",
      "targets": [
        {
          "expr": "vf_browser_session_created_total",
          "legendFormat": "{{mode}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 6, "x": 6, "y": 45 },
      "id": 16,
      "title": "Active Browser Sessions",
      "type": "gauge",
      "targets": [
        {
          "expr": "vf_browser_session_active",
          "legendFormat": "{{mode}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 6, "x": 12, "y": 45 },
      "id": 17,
      "title": "Browser Retries",
      "type": "stat",
      "targets": [
        {
          "expr": "vf_browser_retry_count_total",
          "legendFormat": "{{operation}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 6, "x": 18, "y": 45 },
      "id": 18,
      "title": "Sandbox Created / Deleted",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(vf_sandbox_created_total[5m])",
          "legendFormat": "created"
        },
        {
          "expr": "rate(vf_sandbox_deleted_total[5m])",
          "legendFormat": "deleted"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "s" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 53 },
      "id": 19,
      "title": "Browser Action Duration by Type",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.50, rate(vf_browser_action_duration_bucket[5m]))",
          "legendFormat": "p50 {{action_type}}"
        },
        {
          "expr": "histogram_quantile(0.95, rate(vf_browser_action_duration_bucket[5m]))",
          "legendFormat": "p95 {{action_type}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 53 },
      "id": 20,
      "title": "Browser Action Count by Type",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(vf_browser_action_count_total[1m]) * 60",
          "legendFormat": "{{action_type}}"
        }
      ]
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 61 },
      "id": 105,
      "title": "Tool Calls",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 62 },
      "id": 21,
      "title": "Tool Call Rate by Tool",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(vf_tool_call_count_total[1m]) * 60",
          "legendFormat": "{{tool_name}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "s" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 62 },
      "id": 22,
      "title": "Tool Call Duration by Tool",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.50, rate(vf_tool_call_duration_bucket[5m]))",
          "legendFormat": "p50 {{tool_name}}"
        },
        {
          "expr": "histogram_quantile(0.95, rate(vf_tool_call_duration_bucket[5m]))",
          "legendFormat": "p95 {{tool_name}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 62 },
      "id": 23,
      "title": "Tool Call Errors",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(vf_tool_call_error_total[1m]) * 60",
          "legendFormat": "{{tool_name}} {{error_type}}"
        }
      ]
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 70 },
      "id": 106,
      "title": "Errors & Debugging",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 71 },
      "id": 24,
      "title": "Errors by Category",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(vf_error_count_total[5m])",
          "legendFormat": "{{category}} / {{error_type}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 71 },
      "id": 25,
      "title": "Error Breakdown (pie)",
      "type": "piechart",
      "targets": [
        {
          "expr": "sum by (category) (vf_error_count_total)",
          "legendFormat": "{{category}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 79 },
      "id": 26,
      "title": "Turns per Rollout",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(vf_env_turn_count_total[1m]) * 60",
          "legendFormat": "turns/min"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": { "unit": "s" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 79 },
      "id": 27,
      "title": "Setup Duration",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.50, rate(vf_setup_duration_bucket[5m]))",
          "legendFormat": "p50 setup"
        },
        {
          "expr": "histogram_quantile(0.95, rate(vf_setup_duration_bucket[5m]))",
          "legendFormat": "p95 setup"
        }
      ]
    }
  ],
  "schemaVersion": 39,
  "tags": ["verifiers", "llm", "rl"],
  "templating": {
    "list": [
      {
        "current": {},
        "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
        "definition": "label_values(vf_rollout_count_total, env_id)",
        "name": "env_id",
        "query": "label_values(vf_rollout_count_total, env_id)",
        "refresh": 2,
        "type": "query"
      },
      {
        "current": {},
        "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
        "definition": "label_values(vf_rollout_count_total, model)",
        "name": "model",
        "query": "label_values(vf_rollout_count_total, model)",
        "refresh": 2,
        "type": "query"
      }
    ]
  },
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "Verifiers - Run Dashboard",
  "uid": "verifiers-run-dashboard",
  "version": 1
}
